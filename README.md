## TODO

Avoid recompilation of already-encountered source files
Make file names unique also based on thread ID as two threads might otherwise work on the same files causing race conditions

quotes around $CARGO_MANIFEST_DIR